# 1. Get all files in the directory, keep only ones with ".txt" format
# 2. Separate lines based on "---" into lists of questions.
# 3. For each list of questions, parse question to extract recipe step.
# 4. For all the next lines parse line -  split at ":" make it left into a skill and right into skill note.